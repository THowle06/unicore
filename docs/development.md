# Development Guide

## Prerequisites

- Docker
- Docker Compose
- Node.js 20+
- Python 3.12

## Environment Variables

Copy `.env.example` to `.env` and configure the values appropriately.

## Running the Application

```bash
docker compose up --build
```

## Common Commands

- `docker compose up` - Start services
- `docker compose down` - Stop services
